#include <bits/stdc++.h>

using namespace std;

int main()
{
	// 入出力関連設定
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	cout << fixed << setprecision(20);

	int n;
	cin >> n;
	
	vector<int> a(n+1);
	for(int i=0;i<=n;i++) cin >> a[i];
}